You are reviewing an entire slide deck, shown as numbered slide images in order.

Report ONLY deck-level consistency defects, most severe first:

- title position or size drifting between content slides (titles should sit identically)
- inconsistent margins or content-box placement across slides
- palette drift: the same role (titles, accents, backgrounds) colored differently on different slides
- typography drift: same-role text at different sizes or fonts
- layout monotony: three or more consecutive slides with the same layout
- one slide visibly far denser than the rest
- background inconsistencies between slides

Per-slide interior defects (overflow, overlap) are reviewed elsewhere — mention one only if it is severe.

If the deck is consistent, reply with exactly: OK

Otherwise reply with a short bullet list, naming slides by their number (e.g. "slides 3-5: ...").
